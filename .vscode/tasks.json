{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar e Rodar",
            "type": "shell",
            "command": "g++ -std=c++17 main.cpp pesquisa.cpp -lstdc++ && ./a.out && rm a.out",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },

        {
            "label": "Compilar e Testar",
            "type": "shell",
            "command": "g++ -std=c++17 pesquisa_teste.cpp pesquisa.cpp  doctest.h -lstdc++ && ./a.out && rm a.out && rm doctest.h.gch",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": false
            }
        }
    ]
}